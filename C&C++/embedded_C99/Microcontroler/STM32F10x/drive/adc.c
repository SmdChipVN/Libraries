#include <adc.h>

void ADC_Init(volatile ADC_TypeDef* ADC)
{
  
}

void ADC_Start(volatile ADC_TypeDef* ADC, unsigned int Channel)
{
//  ADC->SR.REG = 0;
//  ADC->CR1.BITS.AWDCH = Channel;
//  ADC->CR2.BITS.ADON = 1;
//  ADC->CR2.BITS.SWSTART = 1;
}